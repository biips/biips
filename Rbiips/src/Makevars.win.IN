PKG_CPPFLAGS = -I"$(BOOSTLIB)" -Icore/include -Ibase/include -Icompiler/include -Icompiler/src @RBIIPS_COMPILE_FLAGS@

## Use the R_HOME indirection to support installations of multiple R version

PKG_LIBS = @RBIIPS_LINK_FLAGS@ $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "Rcpp:::LdFlags()")
