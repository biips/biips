#ifndef __PARSE_VARNAME__
#define __PARSE_VARNAME__
void parse_varname(const string & to_parse,
                  string & var_name,
                  vector<size_t> & lower,
                  vector<size_t> & upper);

                  
#endif
