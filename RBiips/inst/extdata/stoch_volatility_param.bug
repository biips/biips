# Stochastic volatility model

model
{
  alpha ~ dunif(0,0.99)
  prec.x <- (1-alpha^2)/sigma^2
  x[1] ~ dnorm(0, (1-alpha^2)/sigma^2)
  for (t in 2:t.max)
  { 
    x[t] ~ dnorm(alpha*x[t-1], (1-alpha^2)/sigma^2)
    prec.y[t] <- 1 / (beta^2 * exp(x[t]))  
    y[t] ~ dnorm(0, prec.y[t])
  }
}
