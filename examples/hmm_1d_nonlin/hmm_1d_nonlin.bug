var x_true[t_max+1], x[t_max+1], y[t_max]

model
{
  x[1] ~ dnorm(mean_x_init, prec_x_init)
  for (t in 2:t_max+1)
  {
    x[t] ~ dnorm(0.5*x[t-1]+25*x[t-1]/(1+x[t-1]^2)+8*cos(1.2*(t-1)), prec_x)
    y[t-1] ~ dnorm(x[t]^2/20, prec_y)
  }
}
