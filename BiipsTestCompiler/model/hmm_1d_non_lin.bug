var x[1,t.max+1], y[1, t.max]

model
{
  x[,1] ~ dnormvar(mean.x.init, var.x.init)
  for (t in 2:t.max+1)
  {
    x[,t] ~ dnormvar(0.5*x[,t-1]+25*x[,t-1]/(1+x[,t-1]^2)+8*cos(1.2*(t-1)), var.x)
    y[,t-1] ~ dnormvar(x[,t]^2/20, var.y)
  }
}
